# Default values for a java11 app prod environment.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 2

configServer:
  configLabel: prod
  profilesActive: "prod,it"
  configUri: "http://subatomic-config-server.bolt-avaf-shared-prod.svc"
